# ════════════════════════════════════════════════════════════
#  Python dependencies for all app modules
# ════════════════════════════════════════════════════════════

# API framework
fastapi==0.111.0
uvicorn[standard]==0.30.0

# Database
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.30
psycopg2-binary==2.9.9

# Kafka (async producer)
kafka-python==2.0.2
aiokafka==0.10.0

# Redis
redis==5.0.4

# AWS (DynamoDB + S3/MinIO)
boto3==1.34.60
aioboto3==12.4.0

# MinIO SDK (alternative to boto3 for S3-compatible)
minio==7.2.7

# AI — Anthropic
# anthropic==0.28.0

# File format parsing
lxml==5.2.2           # XML
pandas==2.2.2          # CSV + Parquet
pyarrow==16.1.0        # Parquet
python-magic==0.4.27   # Binary file type detection

# Utilities
pydantic==2.7.1
pydantic-settings==2.3.0
python-multipart==0.0.9   # file upload
python-dotenv==1.0.1
httpx==0.27.0
tenacity==8.3.0           # retry logic

# WebSocket (for consumer-hot alerts)
websockets==12.0

# Logging
structlog==24.2.0